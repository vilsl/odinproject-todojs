/* 
This module handles rendering the lists and todos, then pushes
them to the DOM.
*/

// Loops through and parses information from the todo lists
const ListExtractor = (() => {

})();

// Pushes the information to DOM
const ListRenderer = (() => {

})();


